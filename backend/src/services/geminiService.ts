/**
 * @deprecated This file is a compatibility shim.
 * Import from embeddingService directly:
 *   import { getEmbedding, embedChunks } from './embeddingService';
 */
export { getEmbedding as getEmbeddings, getEmbedding, embedChunks } from './embeddingService';
